# Vercel Marketplace PostgreSQL (Neon, Supabase, Aurora 등)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require

# Vercel Blob image storage (injected automatically after connecting a Blob store)
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_REPLACE

# 관리자 계정
ADMIN_USERNAME=ROOT
ADMIN_PASSWORD=ROOT

# 32자 이상의 임의 문자열. 운영 환경마다 서로 다른 값을 사용하세요.
ADMIN_SESSION_SECRET=replace-with-a-random-secret-at-least-32-characters

# Brand Pilot 관리자 API. 브라우저에 노출되는 NEXT_PUBLIC 변수로 만들지 않습니다.
BRAND_PILOT_ADMIN_API_URL=http://127.0.0.1:4000
BRAND_PILOT_ADMIN_API_TOKEN=replace-with-a-separate-service-token

# 선택 사항: Google Analytics 4
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-REPLACE_ME

# 선택 사항: 어드민 GA4 분석 대시보드
GA4_PROPERTY_ID=123456789
GA4_SERVICE_ACCOUNT_EMAIL=analytics-reader@PROJECT_ID.iam.gserviceaccount.com
GA4_SERVICE_ACCOUNT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\nREPLACE_ME\\n-----END PRIVATE KEY-----\\n"
