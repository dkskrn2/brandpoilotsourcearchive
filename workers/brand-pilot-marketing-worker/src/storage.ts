import { put } from "@vercel/blob"; import type { LocalMarketingResult } from "./contracts.js";
export interface MarketingStorage { upload(input: { brandId: string; generationId: string; outputId: string; result: LocalMarketingResult }): Promise<{ manifest: Record<string, unknown>; manifestUrl: string }>; }
export function createStorage(token: string, putImpl: typeof put = put): MarketingStorage { return { async upload({ brandId, generationId, outputId, result }) { const root = `brands/${brandId}/ai-content/${generationId}/marketing/${outputId}`; const asset = await putImpl(`${root}/creative.png`, result.creative, { access: "public", allowOverwrite: true, contentType: "image/png", token }); const manifest = { version: "ai-content.v1", type: "marketing", title: result.title, assets: [{ role: "creative", url: asset.url, fileName: "creative.png", mimeType: "image/png", width: result.dimensions.width, height: result.dimensions.height, index: 1 }], content: result.content }; const stored = await putImpl(`${root}/manifest.json`, JSON.stringify(manifest), { access: "public", allowOverwrite: true, contentType: "application/json", token }); return { manifest, manifestUrl: stored.url }; } }; }
